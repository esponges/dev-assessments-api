import { CreateAssessmentDto } from './create-assessment.dto';

describe('CreateAssessmentDto', () => {
  it('should be defined', () => {
    expect(new CreateAssessmentDto()).toBeDefined();
  });
});
